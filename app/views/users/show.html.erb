<h1> <%= "#{@user.first_name} #{@user.last_name}" %> </h1>

<h2> Playlists </h2>
<% @user.playlists.each do |playlist| %>
    <%= link_to "#{playlist.name}", playlist_path(playlist) %>
<br>
<% end %>
    
<h2> Bookmarks </h2>

<% @user.bookmarks.each do |bookmark| %>
 <!--  < if bookmarkable.is_a? Artist %>
  if bookmarkable.is_a? Song
  if bookmarkable.is_a? Playlist -->
    <%= link_to "#{bookmark.bookmarkable.name}", playlist_path(bookmark) %>

(<%= link_to "Remove Bookmark", playlist_path(bookmark), method: :delete, data: {:confirm => 'Are you sure?'} %>)
<br>
<% end %>
<br>
<h3>
<%= link_to "Edit", edit_user_path(@user) %>
<%= link_to "Delete", user_path(@user), method: :delete, data: {:confirm => 'Are you sure?'} %>
</h3>